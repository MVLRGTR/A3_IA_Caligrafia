{
  "name": "calia",
  "version": "1.0.0",
  "description": "backend da ia para reconhecer as letras",
  "main": "index.js",
  "engines" :{
    "node":"20.x"
  },
  "scripts": {
    "start" : "node index.js",
    "install:venv" : "python3 -m venv venv && venv/bin/activate && pip install -r requirements.txt",
    "postinstall": "npm install && npm run install:venv"
  },
  "author": "Grupo A3",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "multer": "^1.4.5-lts.1"
  }
}
